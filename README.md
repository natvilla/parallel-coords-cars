parallel-coords-cars
====================

Parallel Coordinate code created in Processing2.0

This project was created using Processing2.0. It is code that runs a parallel coordinate visualization on cars data.

